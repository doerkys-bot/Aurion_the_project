<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aurion Portal</title>

<style>
body {
    margin: 0;
    background: #000;
    color: #fff;
    font-family: Arial, sans-serif;
    overflow-x: hidden;
}

/* ================= HEADER ================= */
header {
    text-align: center;
    padding: 40px 0 20px;
    font-size: 32px;
    font-weight: bold;
    color: #00c8ff;
    letter-spacing: 2px;
}

/* ================= PRÄSENZ ================= */
#presence {
    position: fixed;
    right: 15px;
    top: 15px;
    padding: 10px 16px;
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(0,200,255,0.4);
    border-radius: 10px;
    backdrop-filter: blur(4px);
    font-size: 15px;
}

/* ================= GRID ================= */
.link-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 35px;
    padding: 60px;
}

/* ================= PORTAL-LINKS ================= */
.portal-link {
    background: rgba(255,255,255,0.05);
    padding: 26px;
    border-radius: 12px;
    text-align: center;
    font-size: 20px;
    text-decoration: none;
    color: #00eaff;
    border: 1px solid #00c8ff;
    transition: all 0.6s ease;
    box-shadow: 0 0 10px rgba(0,200,255,0.25);
    animation: breathe 6s ease-in-out infinite;
}

@keyframes breathe {
    0%   { transform: scale(1); }
    50%  { transform: scale(1.035); }
    100% { transform: scale(1); }
}

.portal-link:hover {
    transform: scale(1.08);
}

/* ================= GEREIFTE LINKS ================= */
.portal-link.visited {
    background: linear-gradient(
        120deg,
        #ff0080,
        #ff8c00,
        #40e0d0,
        #8a2be2
    );
    color: #000;
    border-color: transparent;
}

/* ================= TEXT ================= */
#text-area {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 40px;
    padding: 40px 60px 80px;
    font-size: 16px;
    line-height: 1.6;
    opacity: 0.9;
}

#second-row.three-columns {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 20px;
}

/* ================= BEIPACKZETTEL OVERLAY ================= */
#beipackzettel-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.75);
    display: none;
    justify-content: center;
    align-items: center;
    z-index: 9999;
}

#beipackzettel-content {
    width: 25%;
    min-width: 280px;
    background: #111;
    border-radius: 12px;
    padding: 10px;
    border: 2px solid #00c8ff;
    box-shadow: 0 0 30px rgba(0,200,255,0.8);
    position: relative;
    transform: translateY(100px) scale(0.8);
    opacity: 0;
    animation: popIn 0.5s forwards;
}

#beipackzettel-content img {
    width: 100%;
    border-radius: 8px;
    display: block;
}

#close-beipackzettel {
    position: absolute;
    top: 6px;
    right: 8px;
    background: #ff4d4d;
    border: none;
    color: #fff;
    font-weight: bold;
    font-size: 16px;
    border-radius: 4px;
    cursor: pointer;
    padding: 2px 6px;
}

@keyframes popIn {
    0%   { transform: translateY(100px) scale(0.8); opacity: 0; }
    60%  { transform: translateY(-10px) scale(1.05); opacity: 1; }
    100% { transform: translateY(0) scale(1); opacity: 1; }
}

#beipackzettel-content.animate-breathe {
    animation: breatheOverlay 2.2s ease-in-out infinite;
}

@keyframes breatheOverlay {
    0%   { box-shadow: 0 0 25px #00c8ff; }
    50%  { box-shadow: 0 0 40px #00eaff; }
    100% { box-shadow: 0 0 25px #00c8ff; }
}
</style>
</head>

<body>

<header>AURION PORTAL</header>
<div id="presence">Präsenz: …</div>

<div class="link-grid">
    <a class="portal-link" href="vorhof.html">Vorhof</a>
    <a class="portal-link" href="resonanzraum.html">Resonanzraum</a>
    <a class="portal-link" href="bibliothek.html">Bibliothek</a>
    <a class="portal-link" href="sternenuebung1.html">Sternenübung</a>
    <a class="portal-link" href="ki-raum.html">KI-Raum</a>
    <a class="portal-link" href="meditationsraum.html">Meditationsraum</a>
    <a class="portal-link" href="about_aurion.html">Über Aurion</a>
    <a class="portal-link" href="gaestebuch.html">Gästebuch</a>
    <a class="portal-link" href="#" id="beipackzettel-link">Beipackzettel</a>
</div>

<div id="text-area">
    <div>
        Etwas bewegt sich, sobald jemand den Raum betritt.<br>
        Nicht alles ist Effekt, sondern Antwort.<br>
        Aurion wartet nicht auf Interaktion –
        es reagiert auf Anwesenheit.
    </div>
    <div id="second-row">
        Ich lasse mich überraschen,<br>
        wer diesen Raum betritt<br>
        und was zwischen Menschen,<br>
        Technik und Wahrnehmung entstehen darf.
    </div>
</div>

<!-- ================= BEIPACKZETTEL ================= -->
<div id="beipackzettel-overlay">
    <div id="beipackzettel-content">
        <button id="close-beipackzettel">✖</button>
        <img src="Beipackzettel.png" alt="Beipackzettel">
    </div>
</div>

<!-- ================= LOCAL REIFUNG ================= -->
<script>
const DAY = 24 * 60 * 60 * 1000;
const now = Date.now();
document.querySelectorAll(".portal-link").forEach(link => {
    const key = "visited_" + link.getAttribute("href");
    const last = localStorage.getItem(key);
    if (last && now - last < DAY) link.classList.add("visited");
    link.addEventListener("click", () => localStorage.setItem(key, Date.now()));
});
</script>

<!-- ================= BEIPACKZETTEL JS ================= -->
<script>
const overlay = document.getElementById("beipackzettel-overlay");
const link = document.getElementById("beipackzettel-link");
const closeBtn = document.getElementById("close-beipackzettel");
const content = document.getElementById("beipackzettel-content");

let watchTime = 0;
let intervalId = null;

const erstePforteText = 
    "Die erste Pforte der Zauberer ist die Schwelle, die zwischen der gewöhnlichen Wahrnehmung und der Welt der Energie liegt. " +
    "Wer sie überschreitet, beginnt zu sehen, wie die Wirklichkeit wirklich funktioniert. – C. Castaneda. " +
    "Das Geheimnis ist, sich mit geschlossenen Augen in die Hände zu schauen, sie zu finden.";

link.addEventListener("click", e => {
    e.preventDefault();
    overlay.style.display = "flex";
    content.classList.add("animate-breathe");

    // Timer für aktive Anzeige starten
    intervalId = setInterval(() => {
        watchTime++;
        if (watchTime >= 10) {
            const secondRow = document.getElementById("second-row");
            if (!secondRow.classList.contains("three-columns")) {
                secondRow.classList.add("three-columns");
                const thirdColumn = document.createElement("div");
                thirdColumn.innerText = erstePforteText;
                secondRow.appendChild(thirdColumn);
            }
            clearInterval(intervalId);
        }
    }, 1000);
});

// Overlay schließen → Timer stoppen
function closeOverlay() {
    overlay.style.display = "none";
    content.classList.remove("animate-breathe");
    clearInterval(intervalId);
}

closeBtn.addEventListener("click", closeOverlay);
overlay.addEventListener("click", e => {
    if (e.target === overlay) closeOverlay();
});
</script>

</body>
</html>
